torch>=2.0.0
transformers>=4.55.2
vllm==0.10.2
accelerate>=0.24.0
sentencepiece>=0.1.99
protobuf>=3.20.0
# 注意：此文件用于VLLM优化推理环境
# VLLM 0.11+版本的V1引擎与Qwen模型不兼容，使用0.10.2版本（稳定且兼容）
# VLLM需要transformers>=4.55.2，与transformers_stream_generator不兼容
# 因此不包含transformers_stream_generator

